# @package data
defaults:
  - /data/_base
  - _self_

name: intphys
root: ???
property: ???

# sampling
frame_step: 2
frames_per_clip: ${eval:99//${data.frame_step}} 

dataloader:
  dataset:
    _target_: intphyseval.data.intphys_dataset.IntPhysDataset
    data_path: ${data.root}/${data.property}


# v3 stuff
batch_size: 16
data_fn:
  _target_:  intphyseval.data.intphys_dataset_v2.get_videos_intphys
  root: ${data.root}
  property: ${data.property}
  meta_path: intphyseval/data/metadata/intphys/dev/meta.csv
  _partial_: True