# @package data
defaults:
  - /data/_base
  - _self_

name: grasp

root: ???
property: ???

# sampling
frame_step: 10

batch_size: 16
data_fn:
  _target_:  intphyseval.data.grasp_dataset_v2.get_videos_grasp
  root: ${data.root}
  property: ${data.property}
  meta_path: intphyseval/data/metadata/grasp/meta.csv
  _partial_: True